pandas>=2.2
numpy>=2.0
scipy>=1.14
python-dotenv>=1.2
pyyaml>=6.0
pytest>=8.0
openpyxl>=3.1
python-docx>=1.2
snownlp>=0.12
zhconv>=1.4
vaderSentiment>=3.3

# Optional, validation-only. The nudge opportunity analysis estimates aspect
# associations with a self-contained Firth penalized logistic regression
# (scripts/build_nudge_opportunity_analysis.py); statsmodels is imported lazily
# only to cross-check that the Firth odds ratios match an ordinary logit on
# well-behaved aspects. The build runs and is reproducible without it (the
# cross-check is skipped gracefully); install it to reproduce that sanity check.
statsmodels>=0.14

# Chinese-specific insight figures are written as SVG with the Python standard
# library and pandas; no matplotlib/seaborn/plotly dependency is required.

# Japanese sentiment runtime has a separate reproducible bootstrap because
# oseti's PyPI metadata depends on legacy `mecab`, which requires system
# `mecab-config` on macOS. Use:
# .venv/bin/python3 scripts/bootstrap_sentiment_environment.py
